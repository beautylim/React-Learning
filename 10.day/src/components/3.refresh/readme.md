# 组件渲染

# 渲染时机
1. 状态更新
2. 父组件有变化，子组件跟着渲染， 比如父组件的某个状态更新了，所有子组件也会重新渲染
3. Context变化， createContext() 值变化，所有消费者都重新渲染
4. 强制渲染

# 优化
* 用useMemo 缓存计算结果，只有当某个state有变化时才重新计算
* useCallback 缓存函数
* React.memo 跳过不必要的渲染
* useReducer 批量更新
* 将组件拆分到不同的独立组件中，组件自己维护变量
* 状态提升，只有使用特定 Context 的组件才会重新渲染

